apiVersion: v1
kind: Service
metadata:
  name: auth-svc
  labels:
    app: rekbers
    service: auth
spec:
  selector:
    app: rekbers
    service: auth
  ports:
    - name: http-port
      port: 3000
